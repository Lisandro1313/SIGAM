# Base de Datos
DATABASE_URL="postgresql://usuario:password@localhost:5432/sigam?schema=public"

# JWT
JWT_SECRET="tu-secreto-muy-seguro-cambialo-en-produccion"
JWT_EXPIRES_IN="7d"

# Email
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="tu-email@municipalidad.gob.ar"
SMTP_PASSWORD="tu-password"
SMTP_FROM="Secretaría de Desarrollo Social <deposito@municipalidad.gob.ar>"

# Email del depósito (donde se envían los remitos)
DEPOSITO_EMAIL="deposito@municipalidad.gob.ar"

# App
PORT=3000
NODE_ENV=development

# Archivos
PDF_STORAGE_PATH="./uploads/pdfs"
MAX_FILE_SIZE=10485760
